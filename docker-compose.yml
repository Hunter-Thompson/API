version: '3'
services:
  database:
    build:
      dockerfile: Dockerfile.dev
      context: ./postgres
  server:
    build:
      dockerfile: Dockerfile.dev
      context: ./node-api-postgres
    volumes:
      - /app/node_modules
      - ./node-api-postgres:/app
    environment:
      - PGPORT=5432
      - PGUSER=hunter
      - PGPASSWORD=helloworld
      - PGDATABASE=api
      - PGHOST=database
    ports:
      - "3000:3000"
 
    