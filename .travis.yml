sudo: required

services:
  - docker

before_install:
  - docker build -t hunter-thompson/legalist-api -f Dockerfile.dev .

script:
  - docker run -d -e CI=true hunter-thompson/legalist-api 
  - bash scripts/test.sh